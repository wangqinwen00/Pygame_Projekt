# 在类的内部，使用 def 关键字来定义一个方法，与一般函数定义不同，类方法必须包含参数 self, 且为第一个参数，self 代表的是类的实例。

# 果然多读两遍定义就可以明白其中的意思

# 类名前面还有模块名，类名第一个字母大写，但模块名第一个字母一般是小写。可能两个就是一样的单词，区别就在于首字母。

# 内存位于计算机主板上，是一种临时存储设备，用于暂时存储正在运行的程序和数据。磁盘是永久性存储设备，用于长期存储文件和数据。在计算机运行时，数据通常从磁盘加载到内存中以供处理，然后在需要时将结果保存回磁盘。

# 内存是计算机中的一种随机访问存储器（RAM），通常是一块硬件芯片，安装在计算机的主板上。

# **要给对象定义属性，在初始化方法中定义

# 游戏框架搭建，一个游戏主程序的职责可以分为游戏初始化和游戏循环


